#include<bits/stdc++.h>
using namespace std;
bool sorted(int arr[], int n){
    if()
    bool restarray=sorted(arr+1,n-1);
    return (arr[0]<arr[1] && restarray);
}
int main(){

    int a[]={1,2,3,4};
    cout<<sorted(a, 4);
    return 0;
}